# Step 0 — Portal Branding

Set portal title to `PlatformCon-Carne Platform`.

## Action

Try updating organization settings via Port MCP if an endpoint exists.

If not available, guide the user manually:

1. Port Builder → Organization settings
2. Set **Title** to `PlatformCon-Carne Platform`
3. Save

## Done when

Portal title shows `PlatformCon-Carne Platform`.

## Fallback

Manual UI only — no JSON file for this step.
